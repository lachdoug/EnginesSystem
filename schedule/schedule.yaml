---
publisher_namespace: EnginesSystem
type_path: schedule
accepts:
- ManagedEngine
- ManagedService
title: schedule
major: 0
minor: 0
release_level: beta
patch: 0
service_container: schedule
service_handle_field: title
persistent: true
consumer_params:
  action_type:
    name: action_type
    value: schedule
    mandatory: true
    immutable: true
    input:
      label: Action type
      type: select
      collection:
        items:
          schedule: Schedule
          action: Action
  cron_job:
    name: cron_job
    mandatory: true
    input:
      label: The action
      type: boolean
  tiltle:
    name: tiltle
    mandatory: true
    immutable: true
    input:
      type: string
  when:
    name: when
    mandatory: true
    input:
      label: Cron when specification
      type: string
      placeholder: "* * * * *"
